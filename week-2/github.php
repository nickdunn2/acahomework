git status

git add . (adds everything instead of individual files)

git commit -m "message" (commits with optional message)

commit now local, still needs to be pushed up to github

git push (will push the commit up to github)

touch test.php (creates file named test.php)

mkdir homework (creates new folder/directory named homework)